"use strict";

module.exports = function (server) {
    // setTimeout(() => {
    //     require("../../services/nginx/nginx").generateConfig().then(() => {

    //     });
    // }, 3000);
};

